import { RPCServer } from '../createRPCServer'
import { PluginHost } from '../../plugin/createPluginHost'

const publishPluginService = (rpc: RPCServer, host: PluginHost) => {
    // TODO
}

export default publishPluginService
